module.exports = {
    DATABASE_HOSTED_URL: process.env.DATABASE_HOSTED_URL
  };






//   try to create a general test point for backend without accesssing db     get('/', (req, res) => {})
//   then with db
